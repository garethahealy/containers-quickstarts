name: Validate

on: [push, pull_request]

jobs:
  conftest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Conftest
        uses: garethahealy/github-actions/confbatstest@policiesarray
        with:
          tests: _test/conftest.sh
          policies: '[{"name": "redhat-cop", "url":"github.com/garethahealy/rego-policies.git//policy?ref=validation-cont"},{"name": "deprek8ion", "url":"github.com/swade1987/deprek8ion.git//policies"}]'